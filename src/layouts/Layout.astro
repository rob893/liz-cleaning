---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const {
  title = 'The Tidy Gal - Cherokee County, GA | Woodstock, Canton, Holly Springs',
  description = 'Professional residential, commercial, and Airbnb cleaning services in Cherokee County, GA including Woodstock, Canton, and Holly Springs. Reliable, detailed, and flexible. Call or email for a free quote.'
} = Astro.props;

const faviconUrl = new URL('/favicon.svg', Astro.site || Astro.url.origin).pathname;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href={faviconUrl} />
    <meta name="generator" content={Astro.generator} />

    <!-- Dark mode script - runs before page renders to prevent flash -->
    <script is:inline>
      // Check for saved preference or use OS preference
      if (
        localStorage.theme === 'dark' ||
        (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)
      ) {
        document.documentElement.classList.add('dark');
      } else {
        document.documentElement.classList.remove('dark');
      }
    </script>

    <!-- SEO Meta Tags -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta
      name="keywords"
      content="cleaning service, Cherokee County, Woodstock, Canton, Holly Springs, residential cleaning, commercial cleaning, Airbnb cleaning, house cleaning, Georgia"
    />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />

    <!-- Structured Data for Local Business -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "LocalBusiness",
        "name": "The Tidy Gal",
        "description": "Professional cleaning services for residential, commercial, and Airbnb properties",
        "address": {
          "@type": "PostalAddress",
          "addressRegion": "GA",
          "addressLocality": "Cherokee County"
        },
        "areaServed": [
          {
            "@type": "City",
            "name": "Woodstock",
            "addressRegion": "GA"
          },
          {
            "@type": "City",
            "name": "Canton",
            "addressRegion": "GA"
          },
          {
            "@type": "City",
            "name": "Holly Springs",
            "addressRegion": "GA"
          },
          {
            "@type": "AdministrativeArea",
            "name": "Cherokee County",
            "addressRegion": "GA"
          }
        ],
        "openingHours": ["Mo 09:00-17:00", "Th 09:00-17:00", "Tu 09:00-12:00", "Fr 09:00-12:00"]
      }
    </script>
  </head>
  <body class="antialiased bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 transition-colors duration-200">
    <slot />
  </body>
</html>
